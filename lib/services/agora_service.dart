/// Public Agora App ID placeholder. RTC tokens are issued only by the backend
/// via [AgoraTokenService] (`POST /api/agora/token`) — never generated in Flutter.
class AgoraService {
  static const String appId = 'YOUR_AGORA_APP_ID';
}
